override public class Kernel32 static stdcall with inner align of

  public class Heap handle; // no finalization

  public out Heap GetProcessHeap;
  public out ref raw HeapAlloc(Heap heap; word flags, bytes);
  public out ref raw HeapReAlloc(Heap heap; word flags; ref raw mem; word bytes);
  public out bool HeapFree(Heap heap; word flags; ref raw mem);

  public class Thread handle;

  public out Thread GetCurrentThreadId;

end;
